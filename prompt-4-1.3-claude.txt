You are Claude Sonnet 4.5. NO LOCAL FILE ACCESS. You only have the text in this prompt.

Task: Produce a minimal unified diff patch for the provided Bash script (install.bash.sh). Do NOT rewrite the whole file. Keep changes as small as possible to avoid regressions.

Constraints:
- Bash 3.2 compatible (macOS default bash).
- Preserve existing flag parsing (-c/-w/-n with case-insensitive long flags). Max 3 flags.
- Keep existing AEON_ROOT logic:
  - Linux/WSL: /opt/aeon
  - macOS: /usr/local/aeon
- Silent mode (-n) must still work as currently specified in the script.
- Do not introduce Docker orchestrator mode. Force orchestrator execution to NATIVE only.

Required changes:
1) Replace the current "download_files" mechanism with a git-based repository sync:
   - Implement function clone_repo() that clones or updates AEON repo into: ${AEON_ROOT}/tmp/repo
   - If repo exists: git fetch --all --prune; git reset --hard origin/main; git clean -fd
   - If repo not exists: git clone --depth 1 <AEON_REPO_URL> <REPO_DIR>
   - AEON_REPO_URL default: https://github.com/conceptixx/AEON.git
2) Remove/disable download_files() and replace call sites with clone_repo().
3) Run orchestrator from the cloned repo path (not from /opt/aeon/library):
   - ORCH: ${REPO_DIR}/library/orchestrator/orchestrator.json.py
   - MANIFEST: ${REPO_DIR}/manifest/manifest.install.json
   - CONFIG: ${REPO_DIR}/manifest/config/manifest.config.cursed.json
   - Execute as aeon-system user:
     sudo -u aeon-system -H AEON_ROOT="${AEON_ROOT}" python3 "$ORCH" --file:"$MANIFEST" --config:"$CONFIG" <forwarded flags>
4) Change ALL log lines produced by install.bash.sh to start with prefix:
   [AEON_BASH]
   Example: [AEON_BASH][INFO] message

Output format:
- First: a short summary of what you changed (max 10 lines).
- Then: a unified diff patch (diff -u) only.

Now here is the current install.bash.sh content:
