Patch install.bash.v7.sh -> install.bash.v7.1.sh.

MUST FIX (Blocker):
1) Call order in main():
   - pre-scan noninteractive stays at top
   - parse_args
   - root check (no log() before logging is configured)
   - detect_os (sets OS_TYPE + AEON_ROOT)
   - setup_logging() MUST be called immediately after detect_os

2) Logging policy:
   - All final logs under ${AEON_ROOT}/logfiles/
   - /tmp only early fallback BEFORE AEON_ROOT is usable
   - Non-silent: after setup_logging do:
       exec > >(tee -a "$LOG_FILE") 2>&1
     so console AND file receive output.
   - Silent (-n): guarantee 0 bytes stdout/stderr by:
       TEMP_LOG="/tmp/aeon-install-$$.log"
       touch "$TEMP_LOG" && chmod 600 "$TEMP_LOG" || true
       exec >"$TEMP_LOG" 2>&1
     After detect_os+setup_logging: migrate TEMP_LOG -> ${AEON_ROOT}/logfiles/install.bash.<ts>.log
     then exec >>"$LOG_FILE" 2>&1 (silent remains file-only).

3) setup_logging must be safe even before aeon-system exists:
   - avoid `id -gn "$AEON_USER"` failing under set -e
   - only chown if user exists; otherwise skip chown.

4) Docker orchestrator mode MUST work:
   - docker run mounts "$AEON_ROOT:/aeon"
   - workdir "-w /aeon"
   - env "-e AEON_ROOT=/aeon"
   - python path "/aeon/library/orchestrator/orchestrator.json.py"
   - manifest args use "/aeon/manifest/..." paths

5) Keep:
   - Bash 3.2 compatibility (no ${var,,})
   - brew never as root (sudo -u BREW_USER -H)
   - flags: only c/w/n, max 3, long flags case-insensitive, unknown -> usage + exit 2
   - orchestrator args forwarding

Output ONLY the full final script (no explanations).